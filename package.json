{
  "name": "mithril-browserify-ts-budo",
  "version": "0.0.1",
  "description": "Mithril / Babel / Browserify / HMR / Budo",
  "main": "index.js",
  "scripts": {
    "compile-ts": "browserify --debug src/index.js -t [ babelify --presets [ es2015 ] ] -o public/js/app.js",
    "compile-pcss": "postcss src/index.pcss --use postcss-import postcss-nesting autoprefixer -o public/css/app.css",
    "watch-pcss": "postcss src/index.pcss --watch --use postcss-import postcss-nesting autoprefixer -o public/css/app.css",
    "compile": "npm-run-all compile-ts compile-pcss",
    "build-ts": "browserify src/index.js -t [ babelify --presets [ es2015 ] ] | uglifyjs -cm -o public/js/app.js",
    "build-pcss": "postcss src/index.pcss --no-map --use postcss-import postcss-nesting autoprefixer postcss-clean -o public/css/app.css",
    "build": "npm-run-all build-ts build-pcss",
    "clean": "rm -f public/js/app.js public/css/app.css",
    "serve": "budo src/index.js:js/app.js -p 3000 --dir public --live=*.{html,css} -- --debug -p [ browserify-hmr ] -p [ tsify --project src/tsconfig.json ]",
    "start": "npm-run-all -p watch-pcss serve"
  },
  "keywords": [],
  "author": "spacejack",
  "license": "MIT",
  "dependencies": {
    "mithril": "^1.1.1"
  },
  "devDependencies": {
    "autoprefixer": "^7.1.1",
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^7.3.0",
    "browserify": "^14.4.0",
    "browserify-hmr": "^0.3.5",
    "budo": "^10.0.3",
    "npm-run-all": "^4.0.2",
    "postcss-clean": "^1.0.2",
    "postcss-cli": "^4.1.0",
    "postcss-import": "^10.0.0",
    "postcss-nesting": "^4.0.1",
    "tsify": "^3.0.1",
    "tslint": "^5.4.3",
    "tslint-microsoft-contrib": "^5.0.0",
    "typescript": "^2.3.4",
    "uglify-js": "^3.0.18"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "es2015"
          ]
        }
      ]
    ]
  }
}
